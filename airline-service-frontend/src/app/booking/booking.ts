export interface Bookings {
    id: number;
    confirmationNumber: string;
    passengerID: number;
    flightID: number;
}

export interface BookingsDTO {
    id: number;
    confirmationNumber: string;
    passengerID: number;
    flightID: number;
}